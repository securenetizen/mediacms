<% if (page) { %>

	import { renderPage } from './static/js/_helpers.js';

	<% if (page.component && page.componentFile) { %>

		import { <%= page.component %> } from './static/js/pages/ProfilePage/<%= page.componentFile %>';

		<% if (page.id) { %>
			renderPage( '<%= page.id %>', <%= page.component %> );
		<% } %>

	<% } %>
	
<% } %>