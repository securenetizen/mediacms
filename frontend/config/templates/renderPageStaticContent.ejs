<% if (page) { %>

	import { renderPage } from './static/js/_helpers.js';

	<% if (page.component) { %>

		import { <%= page.component %> } from './static/js/static_pages/<%= page.component %>';

		<% if (page.id) { %>
			renderPage( '<%= page.id %>', <%= page.component %> );
		<% } %>

	<% } %>
	
<% } %>