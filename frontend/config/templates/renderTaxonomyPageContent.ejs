<% if (page) { %>

	import { renderPage } from './static/js/_helpers.js';

	<% if (page.component) { %>

		import { <%= page.component %> } from './static/js/pages/taxonomies/<%= page.component %>';

		<% if (page.id) { %>
			renderPage( '<%= page.id %>', <%= page.component %> );
		<% } %>

	<% } %>
	
<% } %>